// contains scc_puti
#include <scc.h>
#include <stdio.h>

int main()
{
    // you can add breakpoints only on lines containing code :)
    puts("Hello, Sailor!");

    int a = 34;
    int b = 35;
    scc_puti(a + b);

    return 0;
}
